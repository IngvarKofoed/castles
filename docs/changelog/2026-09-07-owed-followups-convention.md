# The Owed follow-ups convention lands in CLAUDE.md

The root CLAUDE.md now carries the *Owed follow-ups* section and its
required-reading action: `docs/CLAUDE_TODO.md` is the agent-written,
agent-drained list of work the project owes — never a work queue, items
deleted by the change that completes them. The pre-existing file (two
parked player-feedback items from the housing step) was normalized to the
convention's shape. Two stale lines fixed in the same pass: the render
pointer said "chunked instancing", and App versioning claimed no build
exists.

## Detail
- Scaffold audit against the current conventions; all three subtree
  CLAUDE.md files audited clean, so only root changed.
- The TODO items keep their user-feedback provenance: the
  production-control item is queued to graduate into a spec, not to be
  self-started — the convention's not-a-work-queue rule applies.
