# Owed follow-ups

Work this project owes and hasn't done, written and drained by Claude — the rules are
under *Owed follow-ups* in `CLAUDE.md`. Items are deleted when the work lands, so this
is not a record of anything; `docs/changelog/` is. A line you add here is read as a request.

- **The population-cap readout is opaque** — "6 houses but folk 6/17" doesn't
  connect houses to cap in the player's head; the cheapest fix is the House
  panel stating its contribution ("beds 2 — raises the cap by 2"). Player
  feedback after `2026-09-07-housing-and-wanderers`.
- **Production has no brakes: a staffed sawmill converts every log** — the
  stockpile filter fields have shipped since step 2 with their UI deferred,
  and 5b's mill and oven will drain grain and flour the same way, so
  production control (filter toggles, possibly per-workshop targets) must
  land before or with the bread economy. Player feedback after
  `2026-09-07-housing-and-wanderers`; a production-control spec is queued.
